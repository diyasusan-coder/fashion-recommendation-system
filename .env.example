# Copy this file to ".env" and fill in your free API keys.
# The app still runs without these (with graceful fallback text/images),
# but plugging them in makes the demo MUCH better.

# Get a free key at https://console.groq.com
GROQ_API_KEY= 
# Get a free key at https://www.pexels.com/api/
PEXELS_API_KEY= 
